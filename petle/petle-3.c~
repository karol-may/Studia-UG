#include <stdio.h>
int a,b;
void wprowadz_liczby();
int main(){
	wprowadz_liczby();
	if (b<=a){printf("Poprawna relacja podawanych liczb to a < b\n");wprowadz_liczby();}
	for (int i = a+1;i<b;i++){printf("%d \n",i);}
return 0;
}

void wprowadz_liczby(){
	printf("Wpisz dwie liczby a i b: ");
	scanf("%d %d",&a,&b);
}
